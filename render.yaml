services:
  - type: web
    name: trafficwatch-ids
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn web.app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: TRAFFICWATCH_DEPLOYMENT
        value: render
